## rgctl v0.4.12

**Dashboard dist fix** — align embedded WASM package version with the workspace release so `dashboard/wasm/package.json` matches `rgctl` / `rgctl-wasm` crate versions (was stuck at 0.4.10).

```bash
rgctl --version
./scripts/build-dashboard.sh && cargo build --release
```

## Highlights

### Dashboard / WASM dist

- Bump `dashboard/wasm/package.json` to **0.4.12** (in sync with workspace crates)
- Rebuild with `./scripts/build-dashboard.sh` before `cargo build --release` when dashboard assets change

### Also in this release (since v0.4.9)

v0.4.10 and v0.4.11 were not tagged; this release includes their changes:

- **v0.4.10** — PHP Tier 1 language support ([notes](v0.4.10.md))
- **v0.4.11** — extraction depth across Tier 1 languages + GQL verification smoke ([notes](v0.4.11.md))

## Install

| Platform | Asset |
|----------|-------|
| Linux x86_64 | `rgctl-0.4.12-x86_64-unknown-linux-gnu.tar.gz` |
| macOS Apple Silicon | `rgctl-0.4.12-aarch64-apple-darwin.tar.gz` |
| macOS Intel | `rgctl-0.4.12-x86_64-apple-darwin.tar.gz` |
| Windows x86_64 | `rgctl-0.4.12-x86_64-pc-windows-msvc.zip` |

Verify: `shasum -a 256 -c SHA256SUMS.txt`

## Docs

- [Languages](../languages/README.md) — Tier 1 guides + GQL verification queries
- [AGENTS.md](../../AGENTS.md) · [Install](https://shaaf.dev/rgctl/install/)

## Compare

`v0.4.9...v0.4.12`
